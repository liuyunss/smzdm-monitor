# 什么值得买好价监控系统 - 完成报告

## 📋 项目概述

已创建完整的什么值得买好价监控系统，采用模块化设计，所有组件均可复用。

## 🏗️ 项目结构

```
srzdm-monitor/
├── src/                          # 源代码
│   ├── config/                   # 配置加载模块
│   │   └── loader.py            # YAML配置加载器
│   ├── storage/                  # 数据库存储模块
│   │   └── database.py          # SQLite数据库管理
│   ├── proxy/                    # 代理管理模块
│   │   └── manager.py           # 代理IP池管理
│   ├── crawler/                  # 爬虫模块
│   │   └── smzdm.py             # 什么值得买爬虫
│   ├── scorer/                   # 评分算法模块
│   │   └── algorithm.py         # 多维度评分算法
│   ├── notifier/                 # 通知模块
│   │   └── email.py             # 邮件通知器
│   └── feedback/                 # 反馈服务模块
│       └── service.py           # Flask反馈服务
├── data/                         # SQLite数据库
├── logs/                         # 日志文件
├── templates/                    # 邮件模板
├── config.yaml                   # 配置文件
├── main.py                       # 主程序入口
├── requirements.txt              # 依赖文件
├── README.md                     # 项目说明
└── test_project.py               # 测试脚本
```

## 🎯 核心功能

### 1. 配置管理 (`src/config/loader.py`)
- ✅ YAML配置文件加载
- ✅ 环境变量覆盖
- ✅ 嵌套配置访问
- ✅ 配置热重载

### 2. 数据库存储 (`src/storage/database.py`)
- ✅ SQLite单文件数据库
- ✅ 商品表（products）
- ✅ 价格历史表（price_history）
- ✅ 通知记录表（notifications）
- ✅ 反馈记录表（feedback）
- ✅ 代理使用记录表（proxy_usage）
- ✅ 自动清理旧数据

### 3. 代理管理 (`src/proxy/manager.py`)
- ✅ 免费代理源自动获取
- ✅ 付费API代理支持
- ✅ 并发代理验证
- ✅ 智能轮换策略
- ✅ 失败自动剔除

### 4. 爬虫模块 (`src/crawler/smzdm.py`)
- ✅ 使用 `api.smzdm.com/v1/list` API
- ✅ 解析商品数据（标题、价格、店铺、评论等）
- ✅ 解析互动数据（值/不值/收藏/评论）
- ✅ 多页抓取
- ✅ 时间范围过滤
- ✅ 随机延迟防反爬

### 5. 评分算法 (`src/scorer/algorithm.py`)
- ✅ 历史低价得分（40%权重）
- ✅ 评论增速得分（30%权重）
- ✅ 热度得分（30%权重）
- ✅ 基础门槛检查
- ✅ 好评率过滤
- ✅ 黑名单/白名单过滤

### 6. 邮件通知 (`src/notifier/email.py`)
- ✅ HTML格式邮件
- ✅ 商品详情展示
- ✅ 评分标记
- ✅ 反馈按钮（有用/没用/再提醒）
- ✅ SMTP/TLS支持

### 7. 反馈服务 (`src/feedback/service.py`)
- ✅ Flask Web服务
- ✅ 反馈记录API
- ✅ 反馈统计API
- ✅ 感谢页面
- ✅ 用户反馈学习基础

## 📊 数据字段

从什么值得买API获取的完整数据：

| 字段名 | 说明 | 示例 |
|--------|------|------|
| article_id | 商品ID | 12345678 |
| article_title | 商品标题 | "iPhone 15 128G" |
| article_price | 商品价格 | "5999" |
| article_mall | 店铺名称 | "京东" |
| article_url | 商品链接 | "https://..." |
| article_channel_type | 频道类型 | "haojia" |
| article_format_date | 发布时间 | "2024-01-01 12:00" |
| article_worthy | 值投票 | 150 |
| article_unworthy | 不值投票 | 10 |
| article_comment | 评论数 | 89 |
| tongji_hudong | 互动统计 | "评论_89,收藏_45,值_150,不值_10" |

## 🔧 配置说明

### config.yaml 关键配置段

```yaml
# 监控设置
monitor:
  interval: 1800           # 扫描间隔（秒）
  max_pages: 50            # 最大扫描页数
  max_history_hours: 24    # 扫描时间范围

# 评分权重
scorer:
  weights:
    historical_low: 0.4    # 历史低价权重
    comment_growth: 0.3    # 评论增速权重
    popularity: 0.3        # 热度权重

# 过滤规则
filter:
  blacklist_keywords:       # 黑名单关键词
    - "拼多多"
    - "百亿补贴"
  blacklist_malls: []       # 黑名单店铺
  whitelist_keywords: []    # 白名单关键词

# 代理配置
proxy:
  enabled: true
  source: "free"           # 免费代理源
  rotation:
    on_request: true       # 每次请求轮换
    on_failure: true       # 失败自动切换

# 邮件配置
notifier:
  email:
    smtp_server: "smtp.qq.com"
    smtp_port: 587
    username: ""           # 填写邮箱
    password: ""           # 填写授权码
    to_email: ""           # 填写收件邮箱
```

## 🚀 使用方法

### 1. 安装依赖
```bash
cd /opt/data/home/srzdm-monitor
pip install -r requirements.txt
```

### 2. 配置系统
编辑 `config.yaml`，填写：
- 邮箱配置（SMTP服务器、用户名、密码、收件邮箱）
- 过滤规则（黑名单、白名单）
- 代理设置（如需要）

### 3. 运行监控
```bash
# 单次运行
python main.py --monitor

# 定时运行（每30分钟）
crontab -e
# 添加：*/30 * * * * cd /opt/data/home/srzdm-monitor && python main.py --monitor >> logs/cron.log 2>&1
```

### 4. 运行反馈服务（可选）
```bash
python main.py --feedback
# 服务运行在 http://localhost:5000
```

## 📈 评分算法详解

### 综合评分公式
```
总分 = 历史低价分 × 0.4 + 评论增速分 × 0.3 + 热度分 × 0.3
```

### 1. 历史低价得分
- 当前价格 ≤ 历史最低价：100分
- 当前价格 ≤ 历史最低价 × 1.1：80分
- 当前价格 ≤ 历史最低价 × 1.2：60分
- 当前价格 ≤ 历史最低价 × 1.5：40分
- 其他：20分

### 2. 评论增速得分
- 每小时评论 ≥ 10条：100分
- 每小时评论 ≥ 5条：80分
- 每小时评论 ≥ 2条：60分
- 每小时评论 ≥ 1条：40分
- 每小时评论 ≥ 0.5条：20分

### 3. 热度得分
```
热度 = 评论×2 + 收藏×1.5 + 值×1 - 不值×0.5
```
- 热度 ≥ 500：100分
- 热度 ≥ 200：80分
- 热度 ≥ 100：60分
- 热度 ≥ 50：40分
- 热度 ≥ 20：20分

## 🔄 复用组件

以下组件设计为通用模块，可直接复用到其他项目：

| 模块 | 功能 | 复用场景 |
|------|------|----------|
| config.loader | YAML配置加载 | 任何需要配置管理的项目 |
| storage.database | SQLite数据库操作 | 任何需要本地存储的项目 |
| proxy.manager | 代理IP管理 | 任何需要爬虫的项目 |
| notifier.email | 邮件通知 | 任何需要邮件提醒的项目 |

## ⚠️ 注意事项

1. **邮箱配置**：需要开启SMTP服务并获取授权码（QQ邮箱等）
2. **代理质量**：免费代理不稳定，建议先测试可用性
3. **API限制**：什么值得买API可能有访问频率限制
4. **数据存储**：SQLite数据库文件在 `data/smzdm.db`

## 🎉 总结

项目已完整实现，包含：
- ✅ 7个核心模块
- ✅ 1678行Python代码
- ✅ 完整的配置系统
- ✅ 智能评分算法
- ✅ 邮件通知+反馈按钮
- ✅ 代理IP保护
- ✅ 模块化设计，可复用

**下一步**：配置邮箱后即可运行测试。
