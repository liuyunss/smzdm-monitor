# 快速开始指南

## 1. 安装依赖
```bash
cd /opt/data/home/smzdm-monitor
pip install -r requirements.txt
```

## 2. 配置邮箱
编辑 `config.yaml`，填写以下配置：

```yaml
notifier:
  email:
    smtp_server: "smtp.qq.com"  # 或其他邮箱
    smtp_port: 587
    username: "your_email@qq.com"  # 你的邮箱
    password: "your_auth_code"     # 邮箱授权码
    to_email: "target@email.com"  # 收件邮箱
```

## 3. 运行测试
```bash
python test_project.py
```

## 4. 运行监控
```bash
python main.py --monitor
```

## 5. 设置定时任务
```bash
crontab -e
```
添加：
```bash
*/30 * * * * cd /opt/data/home/smzdm-monitor && python main.py --monitor >> logs/cron.log 2>&1
```

## 6. 查看日志
```bash
tail -f logs/smzdm.log
```

## 常见问题

### Q: 邮箱授权码怎么获取？
A: 登录QQ邮箱 → 设置 → 账户 → 开启SMTP服务 → 生成授权码

### Q: 代理不工作怎么办？
A: 编辑 `config.yaml`，设置 `proxy.enabled: false` 关闭代理

### Q: 如何添加黑名单？
A: 编辑 `config.yaml` 的 `filter` 部分：
```yaml
filter:
  blacklist_keywords:
    - "拼多多"
    - "pdd"
  blacklist_malls:
    - "拼多多官方"
```
